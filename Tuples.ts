// const user: (string | number)[] = [1, "ra"];
//order of the data
const user: [string, number, boolean] = ["", 1, false];
const rgb: [number, number, number] = [255, 255, 200];
type User = [number, string];
const newUser: User = [123, "ra"];
console.log(user);
console.log(newUser);

export {};
